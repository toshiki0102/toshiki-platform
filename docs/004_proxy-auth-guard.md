# 004 proxy.ts 認証ガード

`proxy.ts` でトークン更新と `/admin` ルート保護を動作確認する。

---

## TODO

### 実装確認
- [x] `proxy.ts` が `export function proxy` でエクスポートされていることを確認する（`export default` は不可）
- [x] `edge` ランタイムが指定されていないことを確認する（nodejs のみ対応）

### 動作確認
- [x] 未認証状態で `/admin` にアクセスすると `/login` にリダイレクトされることを確認する
- [ ] ログイン済み状態で `/login` にアクセスすると `/admin` にリダイレクトされることを確認する（実ログイン要）
- [ ] トークン期限切れ後に再アクセスした際にトークンが自動更新されることを確認する（実ログイン要）
- [x] `/`（ギャラリー）へのアクセスが認証なしで通ることを確認する

### セキュリティ確認
- [x] `admin/layout.tsx` でも `getClaims()` による二重チェックが機能していることを確認する
- [ ] proxy をバイパスしても layout で弾かれることを手動テストする（実ログイン要）
